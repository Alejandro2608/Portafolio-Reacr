import React from 'react';
import ReactDOM from 'react-dom'; //renderizar paginas en pantalla
import About from './components/About';

//Qué voy a renderizar (el componente, dónde lo voy a renderizar)
ReactDOM.render(<About/>, document.getElementById('app'));